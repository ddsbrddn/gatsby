/**
 * @type {import('gatsby').GatsbyConfig}
 */
module.exports = {
  siteMetadata: {
    title: `Gatsby`,
    siteUrl: `https://www.yourdomain.tld`,
  },
  plugins: [
    module.exports = {
      plugins: ["gatsby-plugin-netlify"],
    }
    
  ],
}
